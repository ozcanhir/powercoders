...Start...

// Define character set 
   lowCase =(abcdefghijklmnopqrstuvxyz);
   upCase = (ABCDEFGHIJKLMNOPQRSTUVXYZ);
   Numbers = (0123456789);
   SpecialChar = (£$&()*+[]@#^-_!?");
   CharCategories = 4;

// Define passLength => 8;

// Define an empty string variable, which will contain password.
   password="";

// Generates a random number between 1 and 4 to choose the character set
    ChoseCharakterGroup= roundCharCategories(*(charCategories-1))
    
// Generate for the selected character set
// Select chooseCharGroup

Print Output 
   
End 
   display password